# Agent

The V1/V2 agent supports basic read-only tool calling, multi-tool orchestration, and answer aggregation for the owner dashboard.

The agent is an internal dashboard feature. It must not be exposed through public Shopify widget APIs.

Public widget behavior should stay customer-safe and RAG-focused.
